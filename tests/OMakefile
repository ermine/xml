TESTS[] =
   test1
   test2
   test3
   light_test

foreach(name, $(TESTS))
   OCAML_OTHER_LIBS = fstream ini_config conversion
   OCAML_LIBS = ../xml
   OCamlProgram($(name), $(name))

testlib: $(TESTS)

clean:
   $(CLEAN) $(TESTS)
